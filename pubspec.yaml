name: driba_os
description: "Driba OS — The App That Replaces Everything"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core (upgraded for Flutter 3.41+)
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  firebase_storage: ^13.0.6
  firebase_messaging: ^16.1.1
  firebase_analytics: ^12.1.2

  # State Management
  flutter_riverpod: ^2.6.1

  # Cloud Functions
  cloud_functions: ^6.0.6

  # Auth Providers
  google_sign_in: ^6.2.1

  # Media
  video_player: ^2.8.2
  cached_network_image: ^3.3.1
  image_picker: ^1.0.7

  # Storage & Cache
  shared_preferences: ^2.2.2
  path_provider: ^2.1.2

  # Utilities
  intl: ^0.19.0
  uuid: ^4.3.3
  url_launcher: ^6.2.4
  share_plus: ^10.1.4
  connectivity_plus: ^6.1.4

  # Fonts
  google_fonts: ^6.2.1

  # HTTP & API
  dio: ^5.4.0

  # JSON
  json_annotation: ^4.8.1

  # Visibility Detection
  visibility_detector: ^0.4.0+2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/animations/
